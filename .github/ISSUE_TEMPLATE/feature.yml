name: Feature
description: Proponé una funcionalidad nueva o un cambio de comportamiento
title: "[Feature]: "
labels: ["enhancement", "status:needs-triage"]
assignees: []
body:
  - type: markdown
    attributes:
      value: |
        Proponé una feature con el contexto suficiente como para que otro
        dev del equipo pueda implementarla sin volver a preguntarte.

  - type: input
    id: area
    attributes:
      label: Área afectada
      placeholder: "area:rented-record, area:notification, ..."
    validations:
      required: true

  - type: dropdown
    id: priority
    attributes:
      label: Prioridad
      options:
        - priority:p0 (crítico, bloquea release)
        - priority:p1 (importante, sprint actual)
        - priority:p2 (nice to have, backlog)
    validations:
      required: true

  - type: textarea
    id: problem
    attributes:
      label: Problema / oportunidad
      description: ¿Qué dolor resuelve o qué valor agrega?
      placeholder: "Hoy, al gestionar una propiedad, ..."
    validations:
      required: true

  - type: textarea
    id: proposal
    attributes:
      label: Propuesta
      description: Cómo lo resolverías vos. Si no sabés, describí el outcome esperado.
    validations:
      required: true

  - type: textarea
    id: alternatives
    attributes:
      label: Alternativas consideradas
      description: "Opcional: qué otras opciones pensaste y por qué descartaste"
    validations:
      required: false

  - type: textarea
    id: acceptance
    attributes:
      label: Criterios de aceptación
      description: Lista concreta de "esto está listo cuando ..."
      placeholder: |
        - [ ] El usuario puede ...
        - [ ] Los tests cubren ...
        - [ ] Los campos opcionales del DTO tienen @IsOptional()
    validations:
      required: true
